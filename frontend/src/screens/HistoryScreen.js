import React from 'react';
import ComingSoon from '../components/ComingSoon';

// Placeholder — full history (past analyses) is a separate task.
const HistoryScreen = () => (
  <ComingSoon
    icon="time-outline"
    title="History"
    message="Your past color analyses will show up here. Coming soon."
  />
);

export default HistoryScreen;
