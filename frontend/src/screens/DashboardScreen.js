import React from 'react';
import ComingSoon from '../components/ComingSoon';

// Stub — the full dashboard (illustration + Start Analyze + help) is built in the
// next task. Kept minimal here so the tab navigator wires up cleanly.
const DashboardScreen = () => (
  <ComingSoon icon="sparkles-outline" title="Dashboard" message="Dashboard coming up." />
);

export default DashboardScreen;
