import React from 'react';
import ComingSoon from '../components/ComingSoon';

// Placeholder — the full product experience is a separate task. (Distinct from
// the existing ProductCatalog screen, which shows analysis-based recommendations
// inside the try-on flow.)
const ProductScreen = () => (
  <ComingSoon
    icon="shirt-outline"
    title="Products"
    message="Curated pieces for your palette will live here. Coming soon."
  />
);

export default ProductScreen;
